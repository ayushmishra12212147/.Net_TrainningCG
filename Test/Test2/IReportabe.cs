using System;

namespace Test2
{
    //a interface which makes all summary to be included in all classes so that all trancation has a description/summary
    public interface IReportable
    {
        string getSummary();
    }
}